generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Quiz {
  id            Int      @id
  question      String   @db.Char(256)
  options       String[]
  correctOption Int      @default(-1)
}
