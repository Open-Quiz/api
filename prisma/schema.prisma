generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Quiz {
  ID       Int      @id
  QUESTION String   @db.Char(256)
  OPTIONS  String[]
}
